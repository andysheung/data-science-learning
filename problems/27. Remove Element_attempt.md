1st attempt
```python
class Solution:

def removeElement(self, nums: List[int], val: int) -> int:

# loop through nums list

nums1=nums

  

j = 0

l = len(nums)-1

k=0 #counter

for i in range(len(nums)):

if nums[i] != val:

nums1[j] = nums[i]

j+=1

k+=1

# else: #== val:

# nums1[l] = 0

# l-=1

nums =nums1

return k
```

2nd attempt 20251225
```python
class Solution:

def removeElement(self, nums: List[int], val: int) -> int:

j = 0 #init pointer

k = 0

nums1= nums

for i in range(len(nums)):

if nums[i] != val:

nums1[j] = nums[i]

j+=1

k+=1

return **k**
```